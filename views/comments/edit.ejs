<% include ../partials/header %>

  <form class="bg-darkO white top10 container" action="/campground/<%=campground_id%>/comments/<%=comment._id%>?_method=PUT" method="POST">
 <div class="container"> 
      <div class="form-row container">
        <div class="form-group col-md-12">
          <label for="comment">Edit your Comment</label>
          <textarea type="text" class="form-control" name="comment[text]"><%=comment.text %></textarea>
          <sub>Please keep your comments family friendly.</sub>
        </div>
      </div>
    <div class="text-right">
      <button class="btn btn-lg btn-secondary btn-dark border bottom10" type="submit">Add Comment</button>
    </div>
</div>
<div class="top10">&nbsp</div>
</form>
</div>
<% include ../partials/footer %>