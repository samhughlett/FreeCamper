<% include partials/header %>

<h1><%=campground.name%></h1>
<img class="card-img-top text-danger text-center mx-auto img-fluid img-thumbnail"  src="<%=campground.image%>" alt="Image failed to load">
<h3> Campground Coords: <%= campground.lat %> <%=campground.long%></h3>
<div class="container text-center">
    <div class="container">
        <div class="row">
            <form method="post" action="#" >
                <div class="col-12">
                    <input class ="form-control" type="text" name="review" placeholder="your review"></input>
                    <button class="btn btn-secondary btn-block">Add Review</button>
                </div>
            </form>
        </div>
    </div>
</div>

<% include partials/footer %>

